import React from "react"
import styled from "styled-components"

const NavMenuList = () => {
  return <Continer>1</Continer>
}

export default NavMenuList

const Continer = styled.div`
  width: 1000px;
  height: 300px;
  background-color: azure;
`
